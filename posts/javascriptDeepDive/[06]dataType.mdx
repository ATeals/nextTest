---
title: "[06]데이터 타입"
description: 자바스크립트 변수
collection: 모딥다
tags: ["Javascript", "모딥다", "이론"]
date: 2023-05-15
img: "https://image.yes24.com/goods/92742567/XL"
---

> 이 글은 **모던 자바스크립트 Deep Dive**를 읽고 정리한 글입니다.

자바스크립트의 모든 값은 데이터 타입을 갖는다.

자바스크립트(ES6)는 7개의 데이터 타입을 제공한다. 7개의 데이터 타입은 원시타입과 객체 타입으로 분류할 수 있다.

### 원시타입

-   숫자
-   문자열
-   불리언
-   undefined
-   null
-   Symbal

### 객체타입

-   객체
-   함수
-   배열

# 숫자

---

자바스크립트에서 숫자는 모두 실수로 만 처리한다.

```jsx
var binary = 0b01000001; //2진수
var octal = 0o101; // 8진수
var hex = 0x41; // 16진수

console.log(binary, octal, hex); // 65, 65, 65
```

즉, 어떤 형식으로 입력해도 모든 결과는 동일하다.

# 문자열

---

자바스크립트에서 문자열은 작은 따옴표(’’), 큰 따옴표(””), 백틱(``)을 이용하여 표현한다.

백틱을 이용하면 문자열을 포맷팅하여 사용할 수 있다.

```jsx
var name = "kim";

console.log(`my name is ${name}`);
```

이는 분문에서 템플릿 리터럴이라 표현한다.

또한 템플릿 리터럴에서는 멀티라인 문자열을 표현할 수 있다.

# 불리언

---

자바스크립트에서 불리언은 true, false로 나타낸다.

# undefined

---

자바스크립트에서 var키워드로 선언한 변수는 암묵적으로 undefined로 초기화된다.

자바스크립트 엔진이 변수를 초기화하는 데 사용하는 undefined를 개발자가 의도적으로 변수에 할당한다면 본래 취지에 어긋날 뿐더러 혼란을 줄 수 있으므로 권장하지 않는다.

또한 undefined는 “정의되지 않음”이다.

# null

---

프로그래밍 언어에서 null은 변수에 값이 없다는 것을 의도적으로 명시할 때 사용한다.

변수에 null을 할당하는 것은 변수가 이전에 참조하던 값을 더 이상 참조하지 않겠다는 의미다. 이는 이전에 할당되어 있던 값에 대한 참조를 명시적으로 제거하는 것을 의미하며, 자바스크립트 엔진은 누구도 참조하지 않는 메모리 공간에 대해 가비지 콜렉션을 수행할 것이다.

# 심벌

---

심벌은 ES6에 추가된 타입으로, 변경불가능한 원시 타입의 값이다.

심벌 값은 다른 값과 중복 되지 않는 유일무이한 값이다. 따라서 주로 이름이 충돌할 위험이 없는 객체의 유일한 프로퍼티 키를 만들기 위해 사용된다.

 <aside className="shadowBottom my-10 py-[20px] px-[10px] dark:bg-[#1E1E1E] bg-[#efefef] rounded-[10px]">
📌 데이터 타입에 따라 확보되는 메모리 공간의 크기
ECMAScript 사양은 문자열과 숫자 타입 외에 데이터 타입의 크기를 명시적으로 규정하고 있지는 않다. 따라서 문자열과 숫자 타입을 제외하고 데이터 타입에 따라 확보되는 메모리 공간의 크기는 자바스크립트 엔진 제조사의 구현에 따라 다를 수 있다.

</aside>

값을 참조하려면 한번에 읽어 들여야 할 메모리 공간의 크기, 메모리 셀의 개수를 알아야한다.

컴퓨터는 변수의 할당된 값의 타입에 따라 그 변수를 해당 값의 타입으로 인식하고, 그 단위로 메모리 공간에 저장된 값을 읽어들인다.

또한 읽어들인 값의 타입에 따라서 메모리 공간에 저장된 2진수 코드를 해당 값으로 인식한다.

# 동적타이핑 언어

---

자바스크립트는 정적 타입 언어(C, Java)와는 다르게 변수를 선언할 때 타입을 선언하지 않는다.

즉, 미리 선언한 데이터 타입의 값만 할당할 수 있는 것이 아닌 어떠한 데이터 타입의 값이라도 자유롭게 할당할 수 있다.

변수의 데이터 타입의 값을 자유롭게 할당하는 것은 편리하다.

하지만 편리한 만큼 위험이 존재한다.

변수의 값은 언제든지 변경될 수 있기 때문에 복잡한 프로그램에서는 변화하는 변수 값을 추적하기 어려울 수 있다.

심지어 자바스크립트는 개발자의 의도와는 상관없이 자바스크립트 엔진에 의해 암묵적으로 타입 변환이 일어난다.(Typescript를 사용하자!)
